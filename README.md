# Nhắc hẹn 30 phút

Ứng dụng web đơn giản gửi nhắc hẹn mỗi 30 phút. Mở file `index.html` trong trình duyệt, bấm **Bật nhắc hẹn**, và cho phép thông báo nếu muốn nhận nhắc trực quan.

## Cách chạy

1. Mở `index.html` bằng trình duyệt.
2. Bấm **Bật nhắc hẹn**.
3. Giữ tab mở để lịch nhắc hoạt động.
